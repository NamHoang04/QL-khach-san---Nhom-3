import { Metadata } from "next"
import { ReactNode } from "react"

export const metadata: Metadata = {
  title: "Quản lý Loại Phòng | Hotel Management",
}

export default function RoomTypesLayout({ children }: { children: ReactNode }) {
  return children
} 